#include <iostream>
#include <string>

int main() {
    std::string source code = "int x = 5";
    for (int i = 0; i < source_code.length(); i++){

        char current_letter = source_code[i];
        std::cout << "Letter at index" << "is: " << current_letter << "\n";
    }
    return 0;
}
